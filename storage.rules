rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files are uploaded/downloaded by the backend. Authorized downloads use
    // short-lived V4 signed URLs rather than broad client bucket permissions.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
